{
  "name": "@control-hub/connectors",
  "version": "0.4.2",
  "private": true,
  "type": "module",
  "bin": {
    "control-hub-opencode": "dist/opencode-collector/index.js"
  },
  "//": "Neither the API nor the worker owns this package, because both read it: the API to validate a configuration and to know how a provider signs, the worker to execute. It depends on the domain and on zod, and on nothing else of ours.",
  "exports": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@control-hub/domain": "workspace:*",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "typescript": "^6.0.0",
    "vitest": "^4.0.0"
  }
}
