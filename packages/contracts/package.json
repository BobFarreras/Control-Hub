{
  "name": "@control-hub/contracts",
  "version": "0.4.2",
  "private": true,
  "type": "module",
  "//": "The job names have a subpath of their own because the web tier takes this package from the root and a bundler resolving `./connector-jobs.js` against a `.ts` file fails the build. Same shape as `@control-hub/config/flags`, for the same reason: a consumer takes what it needs.",
  "exports": {
    ".": "./src/index.ts",
    "./jobs": "./src/connector-jobs.ts",
    "./release": "./src/release.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "typescript": "^6.0.0",
    "vitest": "^4.0.0"
  }
}
