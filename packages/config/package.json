{
  "name": "@control-hub/config",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "//": "The flag registry has a subpath of its own so a consumer can take it without the environment schema, and with it zod. The web tier needs the flags and nothing else.",
  "exports": {
    ".": "./src/index.ts",
    "./flags": "./src/flags.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "typescript": "^6.0.0",
    "vitest": "^4.0.0"
  }
}
